{
  "name": "joyce-letters-poc",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "eleventy --serve",
    "build": "eleventy",
    "postbuild": "pagefind --site _site --output-subdir pagefind",
    "build:search": "npm run build && npm run postbuild"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "pagefind": "^1.1.0"
  }
}
